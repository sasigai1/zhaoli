# 2026-09-03 Daily Notes

## 素笺（go.zip）— Grok App Builder 项目收尾
- 用户从飞书发来 go.zip（file token HNWpbwYQxo0BiHx2xDJc7b4Intf），是 Grok App Builder 导出的「素笺」日程应用
- 位置：/workspace/go-app/（TanStack Start + React 19 + Tailwind v4 + zustand，AI 书记走 xAI grok-4.5，需要 XAI_API_KEY 才有 AI 功能）
- 应用本身功能已完整：今日/月历/提醒/我 四个视图、口述+手写两种录入、日简、疏时、系统通知

## 收尾修复（"没完成"的部分 = 质量门禁全红）
1. `npm test` 8 个失败：scripts/grok-pwa-plugin.test.mjs 的夹具没隔离 cwd，被工作区真实的 src/lib/og/site.json（素笺）和 public/og.jpg 污染 → 加 hermeticRoot()（mkdtemp 空目录）修复，不改运行时
2. lint 1 error：src/lib/app-data/client.server.ts:214 空 catch 块 → 加注释修复
3. startup.sh 硬编码 `cd /workspace` → 改为 `cd "$(dirname "$0")"`

## 验证结果（全绿）
- npm test: 195+45 全过；lint 0 error（1 个 warning 是平台预置文件里的无用 eslint-disable，保留不动）
- typecheck、npm run build（Vercel 产物）通过
- dev (8080) + built (8081) 冒烟：内容正常、无页面错误、无横向溢出；交互 QA（四标签导航 + 手写添加日程）通过

## 环境性误报（非应用缺陷）
- 冒烟 console 报 ERR_CONNECTION_CLOSED：grok.com/extensions.js 在本沙箱不可达（curl 000）
- 冒烟报 BRAND NOTE：brand-check 默认 workspaceRoot=/workspace，这里应用在 /workspace/go-app；`node scripts/brand-check.mjs --root /workspace/go-app` 实际是 ok:true
- baseline "diverges" 是页面实时时钟导致的文本变化，dev/built 的 bodyTextHash 一致

## 其他
- Playwright 浏览器已装：npx playwright install chromium
- dev 服务器留在 8080 端口运行中
