# 2026-09-04

## 日晷 Sundial（cc.zip）APK 交付完成
- 用户请求：分析飞书 cc.zip → 生成 APK + 精美图标 → 发回飞书
- 产物：日晷-v1.0.apk（8.1MB，com.riji.app，versionCode 1，已签名 riji.keystore/riji2026）+ 日晷-图标.png（1024）
- 已通过 lark-cli im +messages-send（user 身份 → ou_f871580957592baf9f4f8b6b35a3d42e = 小青省）发送三条：说明/图标/APK
- SPA QA（scripts/qa-spa.mjs）通过：零后端静态运行，serverFn 404 → 本地解析回退正常
  - 坑1：品牌拉丁文经 CSS uppercase 渲染，innerText 是 "SUNDIAL"，断言需大小写不敏感
  - 坑2：http-server 需 `--proxy "http://127.0.0.1:8090?"` 才有 SPA 路由回退；8090 曾被残留 python3 服务器占用
- 图标：icons/icon-master-v2.jpg（对比度 OK，~6% 深色墨迹），scripts/make-android-icons.py 生成全套 mipmap + 启动屏（边框均值 #F8EFDE）
- 环境大坑：**沙箱被重置** —— /opt/android-sdk、/opt/gradle-8.14.3、JDK21 全没了
  - 重装方案：cmdline-tools（curl dl.google.com）+ sdkmanager 装 platform-tools / platforms;android-36 / build-tools;35.0.0
  - JDK：mise java 17.0.2 可跑 AGP 8.x（JAVA_HOME=~/.local/share/mise/installs/java/17.0.2）
  - Gradle：mise gradle 8.14.5 直接用（wrapper 下载会因不走代理失败）；gradle.properties 加 systemProp 代理 127.0.0.1:18080
  - Playwright：chromium 装完还要 `npx playwright install-deps chromium`（缺 libatk 等系统库）；残留 __dirlock 需手动删
- keystore 位置：cc-app/android/keystores/riji.keystore（后续升级必须同签名）

## 待办
- 无
